services:
  dashboard:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DATA_DIR=/app/data
    volumes:
      - receipt-data:/app/data
    restart: unless-stopped

volumes:
  receipt-data:
